services:
  pokeflix:
    build: .
    container_name: pokeflix-addon
    ports:
      - "7515:7515"
    environment:
      - PORT=7515
      - NODE_ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
